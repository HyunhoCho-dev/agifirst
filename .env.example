# Server Configuration
PORT=3000
NODE_ENV=production

# Note: Users must provide their own Groq API key through the web interface
# This file is just an example of environment variables that can be set
